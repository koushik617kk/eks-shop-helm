image:
  repository: "381492206162.dkr.ecr.us-east-1.amazonaws.com/order-service"
  tag: "800006adc98585652ed41b90450d6d26725af1b1"
  pullPolicy: Always

replicaCount: 3
app:
  version: "v1.0.0"
  cpuBurnSeconds: "0.1"   # Controls CPU load per request — raise this to trigger HPA

resources:
  requests:
    cpu: "100m"
    memory: "128Mi"
  limits:
    cpu: "500m"
    memory: "256Mi"

hpa:
  enabled: true
  minReplicas: 2
  maxReplicas: 15
  targetCPUUtilizationPercentage: 60   # Lower than frontend — order-service scales faster

pdb:
  enabled: true
  minAvailable: 2

namespace: demo
