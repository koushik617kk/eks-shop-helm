apiVersion: v1
kind: Service
metadata:
  name: shop-frontend
  namespace: {{ .Values.namespace }}
  labels:
    app: shop-frontend
spec:
  type: ClusterIP
  selector:
    app: shop-frontend
  ports:
    - name: http
      port: 80
      targetPort: 5000
      protocol: TCP
